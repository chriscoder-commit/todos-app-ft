<div class="todo-container">
  <div>
    <p>Id: <%= @todo.id %></p>
    <p>Name: <%= @todo.name %></p>
    <p>Done: <%= @todo.done %></p>
  </div>
</div>

<%= link_to "Delete", todo_path(@todo.id), method: :delete %>
<%= link_to "Back", todos_path %>